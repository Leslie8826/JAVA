public class Spaceship extends MobileImages{
	/* This class inherits the MobileImages contructor that has 5 parameters from the MobileImages class.
	 * This class also inherits all the other properties of the MobileImages class.
	 * */
	public Spaceship(int first_X, int first_Y, int dX, int delay, int test) {
		super(first_X, first_Y, dX, delay, test);
	}
}

