public class Meteorite extends MobileImages{
	/* This class inherits the MobileImages contructor that has 4 parameters from the MobileImages class.
	 * This class also inherits all the other properties of the MobileImages class.
	 * */
	public Meteorite(int first_X, int first_Y, int dX, int delay) {
		super(first_X, first_Y, dX, delay);
	}
}

